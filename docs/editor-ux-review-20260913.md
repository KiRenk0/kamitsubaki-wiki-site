# 编辑器体验优化 · 本地审核

> 2026-09-14 更新：当前开发分支已支持新建词条与保留原图的小附件。较大原图通过 GitHub 上传，路径和分类见[图片与文件教程](files-and-images.md)。以下首版/日期验收记录描述当时版本，不代表当前功能范围。

本次修改位于 `codex/wiki-editor-pr`，尚未提交、推送或部署。预览沿用本地联调服务 `http://127.0.0.1:4358`。线上 V2.3.0 不受影响。

## 可审核的变化

- `/zh/contribute/edit/` 分为站内编辑器版与 GitHub 流程版。站内版解释登录、选词条、写作、草稿、差异、投稿和跟进；GitHub 版解释 Fork/分支、文件修改、PR、检查和部署。修正三种源语言 FAQ 中过时的账号及草稿说明，生成繁中版本。
- 切换指南不重载页面，URL 记录所选流程，浏览器返回可还原。旧教程锚点仍会展开对应教程；目标文件继续传给编辑器。
- 正文工具栏加入无序/有序列表与缩进控件。支持 `-`、`+`、`*` 后加空格，以及 `1.` / `1)` 后加空格；中文输入法组合输入期间不转换。支持标题和引用标记。
- 正文 Enter 分段，Shift+Enter 保留换行；列表 Enter 续写、空项 Enter 退出、Tab/Shift+Tab 调整层级。斜杠菜单提供独立的两种列表，空块插入时不多留一个空块。
- 列表 Markdown 导出兼容 Chrome 的直接嵌套列表 DOM，保留有序列表起始编号。旧草稿中的列表块自动转为可直接编辑的富文本格式。
- 修复输入后立即撤销失效的问题；撤销/重做后焦点留在正文。格式按钮在光标移动时更新状态。
- 词条选择结果提供类型筛选、多关键词匹配、结果总数、50 条上限提示、失败重试及键盘选择。长标题和路径在侧栏内换行。
- 统一首页账号入口与 LABs/搜索/菜单的尺寸和圆角规则；调整编辑块、侧栏按钮、选择卡片与窄屏工具栏。修正投稿提示条导致手机侧栏重复下移的问题，并增加侧栏关闭按钮、移动端命令按钮及外观选项的可访问名称。

交互参考：[Notion 写作与编辑基础](https://www.notion.com/help/writing-and-editing-basics)、[键盘快捷键](https://www.notion.com/help/keyboard-shortcuts)。沿用 Wiki 的主题与 Markdown 数据格式，没有引入 Notion 数据库或多人实时协作。

## 已完成验证

- 完整 Node 测试 339 项通过，包括嵌套列表、旧列表迁移、输入触发条件、源文往返与五种语言指南。
- Astro check：0 errors，0 warnings（19 项 hints）。
- Chrome 基础 UI 操作：列表自动转换、续行、缩进/取消缩进、退出列表、有序编号、即时撤销/重做、斜杠菜单插入、长路径搜索、方向键选择、两套指南切换及浏览器返回。
- 桌面及 390×844 窄屏；浅/深主题；中、日、英文编辑器可见检查。测试视口与主题已恢复。
- 五种语言指南本地 HTTP 200，均包含两套流程和本语言编辑器链接。
- 未使用 Playwright，未创建真实投稿或修改生产数据。独立的 `localhost` 草稿用于输入测试，原有 `127.0.0.1` 草稿保持不变。
- 最终生产构建：9,314 页面通过；Cloudflare Pages 资源大小审计通过。

## 文字跳动补修

- 工具栏固定单行、统一按钮高度与文字居中；空间不足时工具区横向滚动，弹出菜单独立于滚动区，避免裁切。
- 关闭内容块重建时的位移动画，避免回车、撤销等结构操作使周围文字重复上下移动。
- Chrome 桌面截图确认工具栏保持单行、格式菜单完整展开；Astro check 0 errors / 0 warnings，diff 空白检查通过。此次仅修改本地布局，未重新运行完整生产构建或发布。

- 字体修正：遵照网站原有字体家族，撤回系统字体覆盖。编辑器单独使用 display=swap，使迟到的中文字体分片仍可替换后备字体；其他页面维持原配置。
