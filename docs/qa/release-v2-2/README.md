# V2.2.0 发布前验收 · 2026-09-09

状态：本地修复与发布候选准备完成；**尚未部署，不是生产验收通过证明**。主站与 Worker 开发分支均为 `codex/account-v2`。阅读进度、偏好云同步与全量日志按约定延期。

## 用户问题逐项对应

| 问题 | 修复与本次证据 | 状态 |
| --- | --- | --- |
| 正式 `/admin` 返回 `internal_error` | 实际迁移后的 SQLite 复现 `no such column: s.login_provider`；移除后台用户查询中越域的会话别名。增加真实数据库测试覆盖多身份用户、后台首页、总览、用户接口；浏览器再次打开真实 Worker 首页成功 | 候选已修复，正式站仍待部署 |
| 控制台登录跳回主站 | API 原点仅放行配置中的控制台路径，签名 state 保留原路径、查询与 hash；非法来源/API 路径拒绝。现有完整模拟 OAuth 回调测试保留 | 自动回归通过；真实 OAuth 待验证 |
| 验证页没有浅色版 | 验证页与组件均接收校验后的 light/dark/auto；手机浅深色截图检查通过 | 页面通过；真实 Turnstile 挑战未执行 |
| 页面、控制台设计不统一 | 主站共享样式；Worker 控制台/验证壳复用主站色彩、字体、圆角、边框与按钮。主站 24 个代表路由 × 2 尺寸 × 2 主题，控制台 6 路由 × 2 尺寸 × 2 主题 | 代表模板矩阵通过，不等同逐一人工查看 9,313 页 |
| 登录与关联含糊 | 独立栏目明确“一个站内账号；每个平台最多一个身份；共享资料收藏；不合并两个已有账号”。分别显示 Google/GitHub、当前方式、移除条件 | 五语页面与双身份/单身份状态通过 |
| 登录弹窗遮挡鼠标 | 自定义鼠标进入原生 dialog 顶层，关闭后恢复；确认框同样处理 | 本地 DOM 与交互检查通过 |
| 菜单、失败恢复、危险操作 | 栏目切换保留草稿；账号失败显示失败状态并可手动重试；确认框默认取消、关闭恢复焦点；后台 DELETE 必须精确输入；手机目录点击章节后收起 | 本地交互通过 |

## 验证记录

- 前端：326/326 测试通过；Astro check：0 errors、0 warnings、15 hints；完整构建 9,313 页。
- Worker：206/206 测试通过；Wrangler 4.129.0 dry-run 打包通过。最后中文状态标签变更另通过语法检查和 5 项账号/后台针对性回归。
- 静态资源审计：14,757 文件；最大 `ai-index.json` 22.38 MiB，现有 Pages 资源限制检查通过。
- [主站矩阵](page-matrix.json)：96 个唯一组合，390×844 / 1280×900、浅/深色。均有 main、共享样式变量，未发现页面横向溢出。这是结构检查，结合下列截图抽查，不宣称所有布局像素一致。
- [控制台矩阵](console-matrix.json)：24 个组合，无页面横向溢出。`8801` 使用真实 Worker 与本机 SQLite；`8802` 的 AI Core/运行页仅使用合成服务响应进行视觉检查，不能证明服务器集成健康。
- [语言检查](locale-matrix.json)：英文、日文、台/港繁体账号关联页 390px；简中在主矩阵中。
- 真实本地接口交互：解绑取消/确认与最后身份保护、503 加载失败及恢复重试、昵称和预设头像保存后刷新、跨栏目草稿保留。
- 确认交互：退出其他设备的取消焦点、鼠标节点与滚动锁恢复；后台停用取消；注销输入 `delete` 保持禁用，`DELETE` 才启用，最终取消且不删除数据。
- 控制台：搜索空结果与恢复、目录搜索/清空、手机展开与章节跳转关闭。后台首页 SQL 验收使用真实数据库，补足此前仅模拟展示数据的遗漏。

截图： [账号关联手机](account-identities-light-390.png)、[后台浅色](admin-accounts-light-1280.png)、[后台深色](admin-accounts-dark-1280.png)、[真实数据库后台修复](admin-real-db-fixed-390.png)、[验证浅色](verification-light-390.png)、[验证深色](verification-dark-390.png)、[运行页深色](operations-dark-1280.png)。前两张后台截图早于最后中文状态标签调整。649px 截图保留实际尺寸，不作为 1280px 证据。

## 发布与回滚

1. 独立紧急修复：`fix/admin-home-query`，提交 `e6437d2`，从正式 Worker 基线 `204c305` 创建，仅修 SQL 和增加回归测试；200 项测试和 dry-run 通过。它可以先行恢复后台，不携带 V2.2.0 功能。
2. 完整版本：先 Worker，再 Pages；本波不新增 D1 迁移，不改 Gateway。版本 V2.2.0；五语公告 `2026-09-09-v2-2-0` 与版本元数据已经就绪。
3. 正式基线已实时核对：Worker 源码 `204c305886473ace9d22dbb621c320eaf1ab793b`，线上版本 `c2f4cf06-f7e4-40a6-89fc-7da9804573a4`，部署 `96daaaaa-075f-4700-9cd0-517429b39af5`；主站远端 main 为 `43ac37ac`。实际发布前再次读取部署，防止期间发生变化。
4. 发布后检查：首页、词条、账号、匿名阅读；Access 认证后后台六入口及总览接口；Google/GitHub 分别完成登录回调，控制台留在原后台路径，主站回原页面；浅/深色 Turnstile；资料/收藏/设备/导出与注销申请取消。
5. 回滚：前端恢复发布前 Pages 部署；Worker 恢复刚确认的上一正常版本。当前基线有已知后台 SQL 缺陷，因此若已先上紧急修复，完整版本回滚应优先回到该修复版本，不回到已知 500 基线。保留现有数据，不回滚/删除数据库。

## 未完成的生产验收

当前浏览器访问正式后台停在 Cloudflare Access 登录页；未完成真实授权或挑战，未改变 Access 策略。候选仍未部署，因此不宣称真实 OAuth 回跳、正式后台恢复或 AI Core 服务集成已验收。第三方 Google/GitHub/Cloudflare 托管登录界面由对应平台控制，本地主题验收针对本站生成的控制台与安全验证页。
