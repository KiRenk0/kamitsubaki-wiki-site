# 实时活动 API 基地址（PUBLIC_ 变量会编译进公开 JS，严禁放任何密钥）
# 只填基地址：不带末尾 /，也不要包含 /v1/events。
#
# 正式部署固定使用公网 HTTPS 域名（与代码内置默认值一致，也可不配置直接走默认值）。
# API 实际运行在本地或云服务器均可：由 api.kamitsubaki.wiki 的
# DNS / 反向代理 / 隧道（cloudflared、frp 等）路由到实际实例，前端无需改动。
PUBLIC_EVENTS_API_BASE=https://api.kamitsubaki.wiki

# 本地联调时改为指向本机服务，部署前务必恢复：
# PUBLIC_EVENTS_API_BASE=http://localhost:8787
