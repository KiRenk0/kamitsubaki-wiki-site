# Site Content

Site files contain global page chrome for each locale:

- navigation labels
- language switcher labels
- hero copy
- section headings
- footer links and disclaimers

Files:

```text
zh.json
ja.json
en.json
```

Keep `supportedLocales` consistent across all three files.

